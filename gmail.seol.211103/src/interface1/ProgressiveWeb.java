package interface1;

/*
 * ProgressiveWeb 클래스는 상속 관계를 만들기 위해서 만든 클래스이지 
 * 실제 사용을 하기 위해서 만든 클래스가 아님.
 */
public interface ProgressiveWeb {
	//interface에는 abstract 메서드가 없어도 추상메서드가 된다
	public void nextPage();

}
